<% if feed_items.any? %>
    <div class="microposts">
      <%= render feed_items %>
    </div>
    <%= will_paginate feed_items, renderer: BootstrapPagination::Rails, class: 'apple_pagination' %>
<% end %>